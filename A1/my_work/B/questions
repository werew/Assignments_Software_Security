    Only letter names
    Trailing chars (after name, ex: i 23 luigi blablabla...) (TAKE ALL)

# C standard (POSIX? C89 (ansii C)?) // note: flag -std=c99

    Negative ages (NO)

Can we make a selections of bugs (we found too many and they don't fit in 2 pages) ?

    Can we change the protorype of a function (ex: sortedcontainer_insert fails silently
if node_new return NULL) ? (PUT COMMENT)

    Are we free in the implementation of sortedcontainer_erase ? (NO BALANCE, FREE ORDER)

    There are more than two problems in read_command, but you say we can change only two lines. (FIX ALL)

    C? POC? (OK LIKE THIS)

